{
  "name": "load-machine",
  "version": "1.0.0",
  "description": "tyring to make lambda fast and take more load the expected",
  "main": "lambda.js",
  "scripts": {
    "clean": "rm -rf ./dist/ ./script/setup/lambda.zip",
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc",
    "zip": "zip -r -j ./script/setup/lambda.zip ./dist/"
  },
  "author": "bunny",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "zip-build": "^1.8.0"
  }
}
